require 'pp'
target = Rails.env
if ENV['DEVELOPER']
  target = ENV['DEVELOPER']
end
APP_CONFIG = HashWithIndifferentAccess.new YAML.load_file("#{Rails.root}/config/<%=name%>.yml")[target]
pp "/* Loaded config for #{target.upcase} * * * * * * * * *  "
pp APP_CONFIG
pp "/* END  * * * * * * * * * * * * * * * * * * * * * * * *  "
